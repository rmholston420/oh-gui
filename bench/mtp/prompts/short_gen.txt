Write a Python function `merge_intervals(intervals)` that merges overlapping closed intervals given
as a list of (start, end) tuples and returns them sorted by start. Handle the empty list. Include a
docstring and three assert-based examples. Do not explain the code afterwards.
