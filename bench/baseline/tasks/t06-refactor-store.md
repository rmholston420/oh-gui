# t06 — Extract a repository interface

Introduce an abstract base for note storage and make `NoteStore` implement it, without changing any
existing test. The app must depend on the abstraction.

Category: refactor across multiple files, no behavior change.
