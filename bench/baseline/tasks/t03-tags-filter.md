# t03 — Filter the list endpoint by tag

`GET /notes?tag=x` should return only notes carrying that tag. Absent parameter returns everything.
Add tests.

Category: additive feature touching request parsing.
