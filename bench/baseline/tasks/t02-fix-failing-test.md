# t02 — Make the suite pass

`pytest` currently passes. Introduce nothing; instead, add a test asserting that `search` is
case-insensitive, watch it fail, and then make it pass.

Category: red-green, cross-file (test drives a source change).
